<tr>
  <td><%= todo.item %>
  <td><%= todo.todo_timer %></td>
    <% if policy(@list).destroy? %>
  <td><%= link_to "Complete", [@list, todo], method: :delete, class: 'btn btn-success btn-sm btn-todo' %></td>
    <% end %>
</tr>
